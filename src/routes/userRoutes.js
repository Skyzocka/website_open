const express = require('express');
const router = express.Router();

// Here we define the user some user-related routes
router.get('/', (req, res) => {
  // Get all users logic
});

// Export the router
module.exports = router;